{% assign listing_cols = '1' %}
{% assign image_size = 'original' %}
   {% if settings.display_bestseller %}
        {% if settings.display_bestseller != blank %}



<ul class="pdt-list products-grid">

    {% for product in collections[settings.home-tab-1-category].products limit:4 %}
                 <li class="item">
    
        {% include 'product-grid-item' with 'listing_cols' with 'image_size' %}
                   </li>
                
      {% endfor %}

</ul>

 
  {% endif %}
  {% endif %}
